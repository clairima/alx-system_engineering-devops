#!/usr/bin/env bash

# Use 'ip' command to get active IPv4 addresses and extract only IPv4 addresses using grep
active_ips=$(ip -4 addr show | grep -oP '(?<=inet\s)\d+(\.\d+){3}')

echo "$active_ips"
